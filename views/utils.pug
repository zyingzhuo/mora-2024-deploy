mixin field(labelText, fieldName, fieldValue, fieldType = 'text', placeholder)
  div(class='form-group')
    label(for=fieldName class='label-control')= labelText
    input(type=fieldType id=fieldName.concat(`${Math.random()}`) name=fieldName value=fieldValue placeholder=placeholder class='form-control')
